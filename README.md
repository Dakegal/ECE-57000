# ECE-57000
# Current implementation is incomplete. Training and evaluation set up for both steps
# Deployable version, in the "runs" section at the bottom of the document, had the goal to load pretrained weights and have images or videos as input to output masks and boxes on the image or live feed.

#Training can be done by running all cells sequentially
